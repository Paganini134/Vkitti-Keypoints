# VKITTI Clone Pseudo Keypoint Results

Static GitHub Pages report for pseudo keypoint generation on selected VKITTI clone car tracks.

Open `index.html` to browse interactive 3D point clouds, fixed-view screenshots, RGB crops, instance-mask crops, normalized point inputs, and predicted keypoints.

The site is self-contained: linked RGB images, masks, viewer HTML files, and prediction arrays are included in this repository.

